version: 2
project_name: mdefaults
builds:
  - id: mdefaults
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - "-s -w"
archives:
  - format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - LICENSE
      - README.md
release:
  github:
    owner: fumiya-kume
    name: mdefaults
  draft: true 
brews:
  - repository: 
    # owner: fumiya-kume
    name: homebrew-mdefaults
    # taps:
    #   - fumiya-kume/homebrew-mdefaults
